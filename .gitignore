data*

.env
.env.web
Revolt.toml
livekit.yml
Revolt.toml.old
livekit.yml.old
secrets.env
secrets.env.old
stoat.json

# Versionado neste fork (branch home-server) para Traefik/Portainer.
# O generate_config.sh oficial sobrescreve este arquivo — rode-o só em /tmp no servidor.
# compose.override.yml
compose.override.yml.old
